---
alwaysApply: false
description: BMS 主管端 UI 设计规范（仅样式排版菜单），参考 https://bms.mtxnyjs.com/front/clocation/admin.html
---
# BMS 主管端 UI 设计规范（样式 / 排版 / 菜单）

> 参考来源：明唐 BMS 大数据系统主管端 https://bms.mtxnyjs.com/front/clocation/admin.html  
> **仅参考样式、排版、菜单结构，不参考具体业务内容**

## 1. 整体风格

- **主题**：深色主题（Dark Theme），专业、科技感
- **布局**：卡片化模块布局
- **气质**：工业监控、数据大屏、B 端管理后台

## 2. 色彩体系

### 2.1 背景色（对齐参考站）
| 用途 | 颜色值 | 说明 |
|------|--------|------|
| 主背景/头部/菜单 | `#0a0e17` | 深碳灰，统一头部与主内容 |
| 卡片背景 | `#1c2128` / `#21262d` | 略浅于主背景 |
| 表单/输入区 | `#21262d` / `#303030` | 输入框、搜索框 |

### 2.2 文字色
| 用途 | 颜色 | 说明 |
|------|------|------|
| 主文字 | `#e6edf3` / `#ffffff` | 标题、重要信息 |
| 次文字 | `#8b949e` | 说明、辅助 |
| 弱化文字 | `#6e7681` | 占位符、禁用 |

### 2.3 强调色与按钮
| 用途 | 颜色 |
|------|------|
| 主色/主按钮/菜单激活 | `#00bcd4`（青色） |
| 链接 | `#58a6ff` 或 `#00bcd4` |
| 成功 | `#4caf50` |
| 警告 | `#d29922` |
| 危险 | `#f44336` |

### 2.4 头部与菜单字号（参考站）
| 区域 | 字号 | 说明 |
|------|------|------|
| 系统名/Logo 旁 | 24px | 加粗 |
| 主导航菜单项 | 16px | 当前项下划线主色 |
| 顶部右侧链接/用户 | 12–14px |

### 2.5 图表配色
- 多类型区分：蓝、绿、黄、紫、橙、红等
- 数值梯度：蓝色渐变（浅→深）

## 3. 布局结构

### 3.1 顶部栏（Header）
```
[ Logo + 系统名称 + 用户/客户标识 ] | [ 快捷入口 ] | [ 统计信息 ] | [ 语言 ] | [ 用户下拉 ]
```
- 左：系统标题 + 当前身份
- 中右：快捷入口（可带标签如 HOT）
- 右：统计、余额、语言、用户菜单

### 3.2 主导航（Nav）
- 水平选项卡
- 当前选中：下划线或背景色高亮
- 右侧：搜索框 + 快捷搜索按钮组

### 3.3 主内容区
- **首页**：多卡片网格
- **列表页**：左树形 + 右主内容（筛选区 + 表格区）
- **详情/监控**：全屏或分栏

### 3.4 浮动面板
- 右下角：可展开/收起的浮动面板，带刷新倒计时

## 4. 菜单结构

```
顶部栏
├── 左侧：Logo + 系统名 + 用户标识
├── 中部：快捷入口
└── 右侧：统计 | 余额 | 语言 | 用户

主导航（水平 Tab）
├── 菜单项 1
├── 菜单项 2
├── ...
└── 右侧：搜索框 + 搜索按钮组

主内容
├── 首页：卡片网格
└── 列表页：左树 + 右表格
    ├── 左侧：树形列表 + 搜索 + 添加
    └── 右侧：Tab 筛选 + 搜索表单 + 状态单选组 + 操作按钮组 + 表格 + 分页
```

## 5. 样式规范（通用）

### 5.1 卡片
- 背景：深灰 `#1c2128`，圆角 `8px` ~ `12px`
- 内边距：`16px` ~ `24px`
- 标题：左上角加粗，字号 `16px`
- 边框：`1px solid rgba(255,255,255,0.06)` 可选

### 5.2 表格
- 表头：背景略深，支持列排序
- 行：hover 高亮
- 操作列：文字按钮，不喧宾夺主

### 5.3 搜索/筛选
- 搜索：输入框 + 图标 + 搜索/高级搜索按钮
- 筛选：单选组，可显示数量

### 5.4 按钮
- 主按钮：蓝色背景
- 次级按钮：描边或透明背景

### 5.5 表单
- 输入框：深色背景，浅色边框
- 下拉：支持搜索、可清空
- 开关：布尔配置

### 5.6 树形
- 可折叠树
- 节点可带数量标识
- 支持搜索、添加

### 5.7 分页
- 每页条数选择 + 页码 + 跳转

## 6. 图标与响应式

- 图标：线性或轻量填充，16px / 20px / 24px
- 最小宽度：≥ 1280px
- 图表、表格随容器自适应
